import { ViewModePermissionDirective } from './view-mode-permission.directive';

describe('ViewModePermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewModePermissionDirective();
    expect(directive).toBeTruthy();
  });
});
